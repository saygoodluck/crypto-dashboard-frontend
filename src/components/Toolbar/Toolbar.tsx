import React from "react";
import "./Toolbar.css";
import SearchTab from "../SearchTab/SearchTab.tsx";

const Toolbar = () => {
    return (
        <header className="toolbar">
            {/*<div className="toolbar__logo">MyApp</div>*/}
            {/* <SearchTab /> */}
        </header>
    );
};

export default Toolbar;
